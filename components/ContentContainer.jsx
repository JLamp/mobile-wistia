import styled from 'styled-components';

export const ContentContainer = styled.div`
  max-width: 500px;
  margin: 0 auto;
  background: var(--grey100);
  height: auto;
  min-height: 100vh;
  padding-bottom: 64px;
`;
